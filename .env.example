# MySQL cache password (only secret — host/user/db are in stock_detect/config.py)
# Use quotes if password contains % or + characters
MYSQL_PASSWORD="your-password-here"

# X Bearer Token — do NOT commit; add GitHub Secret X_BEARER_TOKEN for CI
X_BEARER_TOKEN=
