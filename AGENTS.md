# Agent Notes

- Prefer doing safe repo/terminal operations directly instead of asking the user to run commands.
- Ask only when an action needs credentials, destructive changes, or a choice the repo cannot answer.
